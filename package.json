{
  "name": "oracle-excel-report-service",
  "version": "1.0.0",
  "private": true,
  "description": "Generate monthly Excel reports from Oracle data.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "cli": "node src/cli.js",
    "check": "node --check src/cli.js && node --check src/report-engine.js && node --check src/report-rules.js && node --check src/server.js",
    "test": "npm run check && node --test"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "oracledb": "^6.8.0"
  }
}
